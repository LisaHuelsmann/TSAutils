% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fracdiff.R
\name{fractional_difference}
\alias{fractional_difference}
\title{Fractionally difference a time series}
\usage{
fractional_difference(x, d, max_lag = 30)
}
\arguments{
\item{x}{Numeric vector or time series. Input time series.}

\item{d}{Numeric. Differencing order. Integer values reproduce ordinary
differencing; fractional values allow fractional differencing.}

\item{max_lag}{Non-negative integer. Maximum lag used in the finite
approximation of the infinite expansion.}
}
\value{
A time series object containing the fractionally differenced values.
}
\description{
Applies the fractional differencing operator \eqn{(1 - B)^d} to a
univariate time series.
}
\details{
Fractional differencing is defined by
\deqn{
(1 - B)^d x_t =
\sum_{k = 0}^{\infty} (-1)^k \binom{d}{k} x_{t-k}.
}

This function approximates the infinite sum by truncating it after
\code{max_lag} lags. The first \code{max_lag} observations are removed because they
are affected by zero-padding in the backshift operation.

Positive values of \code{d} correspond to differencing. Negative values correspond
to fractional summation.
}
\examples{
x <- rnorm(200)
y <- fractional_difference(x, d = 0.4)
plot(y)

}
