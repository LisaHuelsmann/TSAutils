% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deseason.R
\name{deseason}
\alias{deseason}
\title{Remove the seasonal cycle from a time series}
\usage{
deseason(t, x, d = 365)
}
\arguments{
\item{t}{Numeric vector of time values expressed as decimal years, for
example \code{2001.5}. The fractional part is used to determine the position
within the year.}

\item{x}{Numeric vector of observations to be deseasonalized.}

\item{d}{Integer. Number of seasonal bins per year. Defaults to \code{365}, which
is suitable for daily data.}
}
\value{
A numeric vector of the same length as \code{x}, with the estimated
seasonal cycle removed and the overall mean added back.
}
\description{
Removes the mean seasonal cycle from a numeric time series by grouping
observations into equally spaced bins within the year.
}
\details{
For each observation, the function extracts the fractional part of \code{t} and
assigns the observation to one of \code{d} equally spaced seasonal bins. The mean
of each bin is used as the estimated seasonal component. The deseasonalized
value is calculated as:

\code{x - seasonal_mean + overall_mean}

This preserves the overall mean of the original series while removing the
average seasonal pattern.
}
\examples{
x_deseason <- deseason(meteodata$dec_year, meteodata$Tmean)
plot(meteodata$Date, meteodata$Tmean, type = "l")
lines(meteodata$Date, x_deseason, col = "red")

}
